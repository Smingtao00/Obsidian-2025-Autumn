## 📚 NumPy 核心概念与速查手册

### 1. 数组 (ndarray) 创建

NumPy 的核心是 `ndarray` 对象。

`ndarray` 是一个（通常固定大小的）多维容器，用于存储相同类型和大小的元素。数组的维度和元素数量由其维度定义`shape`，维度是一个`tuple`，包含 N 个非负整数的数组，分别指定每个维度的大小。数组中元素的类型由一个单独的数据类型对象（dtype）指定，每个 ndarray 都关联一个 dtype 对象。

[`ndarray`](https://numpy.org/doc/stable/reference/generated/numpy.ndarray.html#numpy.ndarray "numpy.ndarray")与 Python 中的其他容器对象一样，可以通过[索引或切片](https://numpy.org/doc/stable/reference/routines.indexing.html#arrays-indexing)数组（例如，使用 N 个整数）来访问和修改数组的内容 ，也可以通过数组的方法和属性来访问和修改数组的内容 。

不同的 `ndarrays` 可以共享相同的数据，因此在一个 ndarray 中所做的更改可能会在另一个 ndarray 中生效。也就是说，一个 ndarray 可以作为另一个 ndarray 的 “视图” ，而它所引用的数据由 “基础” ndarray 负责管理。ndarray 也可以是 Python 拥有的内存视图[`strings`](https://docs.python.org/3/library/stdtypes.html#str "（Python 版本 3.13）")，或者指向实现了[`memoryview`](https://docs.python.org/3/library/stdtypes.html#memoryview "（Python 版本 3.13）")` [Array`](https://numpy.org/doc/stable/reference/arrays.interface.html#arrays-interface)接口的对象。

|**函数**|**描述**|**示例**|
|---|---|---|
|`np.array()`|从 Python 列表/元组创建数组。|`np.array([1, 2, 3])`|
|`np.zeros()`|创建指定形状的全零数组。|`np.zeros((2, 3))`|
|`np.ones()`|创建指定形状的全一数组。|`np.ones((2, 3))`|
|`np.empty()`|创建指定形状的空数组（值未初始化）。|`np.empty((2, 3))`|
|`np.arange()`|创建等差数列数组。|`np.arange(0, 10, 2)`|
|`np.linspace()`|创建指定间隔内均匀分布的 $N$ 个点。|`np.linspace(0, 1, 5)`|
|`np.eye()`|创建单位矩阵（主对角线为 1）。|`np.eye(3)`|

---

### 2. 数组属性

查看数组的基本信息。假设 `arr = np.array([[1, 2], [3, 4]])`。

|**属性**|**描述**|**示例 arr 的结果**|
|---|---|---|
|**`.shape`**|数组的维度（行数、列数等）。|`(2, 2)`|
|**`.ndim`**|数组的维数（轴数）。|`2`|
|**`.size`**|数组中元素的总个数。|`4`|
|**`.dtype`**|数组中元素的类型（如 `int64`, `float32`）。|`dtype('int64')`|

---

### 3. 索引与切片

NumPy 的索引和切片类似于 Python 列表，但支持多维和布尔索引。

|**操作**|**描述**|**示例**|
|---|---|---|
|**基本索引**|访问单个元素。|`arr[0, 1]`|
|**切片**|访问子数组（不包含结束索引）。|`arr[0:2, 1]`|
|**布尔索引**|根据条件选取元素。|`arr[arr > 2]`|
|**花式索引**|使用整数数组指定索引。|`arr[[0, 1], [1, 0]]`|

---

### 4. 形状操作 (Reshaping)

改变数组的维度和形状。

|**函数**|**描述**|**示例**|
|---|---|---|
|**`.reshape()`**|改变数组形状，元素总数不变。|`arr.reshape(4, 1)`|
|**`.ravel()`**|将多维数组展平为一维数组（视图）。|`arr.ravel()`|
|**`.flatten()`**|将多维数组展平为一维数组（副本）。|`arr.flatten()`|
|**`.T`**|数组转置（行列互换）。|`arr.T`|
|**`np.vstack()`**|按垂直方向堆叠数组（行增加）。|`np.vstack([a, b])`|
|**`np.hstack()`**|按水平方向堆叠数组（列增加）。|`np.hstack([a, b])`|

---

### 5. 通用函数 (Universal Functions, ufuncs)

ufuncs 对数组中的每个元素进行操作，速度极快。

|**数学运算**|**描述**|**比较/逻辑运算**|**描述**|
|---|---|---|---|
|`np.sqrt()`, `np.log()`|元素平方根，自然对数。|`np.all()`|**所有**元素为 True。|
|`np.exp()`, `np.sin()`|指数函数，三角函数。|`np.any()`|**任一**元素为 True。|
|`np.add()`, `np.subtract()`|对应元素相加/相减。|`==`, `>`, `<`|元素级比较，返回布尔数组。|

`np.all()` ：
- `np.all(np.diag(matrix) == 1)` 用于检查一个二维数组（或矩阵）的**主对角线上的所有元素是否都等于 1**。
	1. `np.diag(matrix)` 提取主对角线，返回一个**一维数组**，包含对角线上的值
	2. `... == 1` 将上一步得到的 1D 数组中的每个元素与标量 `1` 进行比较，返回一个 ***一维布尔数组***
	3. `np.all(...)` 检查输入数组中的**所有**布尔值是否都为 `True`
- `np.all(matrix == matrix.T)` 检查矩阵 `matrix` 是否完全等于其转置矩阵
	1. `matrix.T` 计算 `matrix` 的转置矩阵
	2. `matrix == matrix.T` 执行***元素级比较***。它返回一个与原矩阵形状相同的布尔数组，其中 `True` 表示原矩阵中的元素等于转置矩阵中对应位置的元素。


### 6. 统计与聚合运算

| **函数**                           | **描述**       | **参数**                          |
| -------------------------------- | ------------ | ------------------------------- |
| **`.sum()`**                     | 元素的总和。       | `axis=0` (按列求和)，`axis=1` (按行求和) |
| **`.mean()`**                    | 元素的平均值。      | 同上                              |
| **`.std()`**                     | 元素的标准差。      | 同上                              |
| **`.min()`**, **`.max()`**       | 元素的最小/最大值。   | 同上                              |
| **`.argmax()`**, **`.argmin()`** | 最小/最大值元素的索引。 | 同上                              |
| **`np.dot()`**                   | 矩阵乘法（或向量点积）。 | -                               |

---

## 7. 高级应用：线性代数 (linalg)

NumPy 提供了强大的线性代数功能。

|**函数**|**描述**|
|---|---|
|`np.linalg.det(a)`|矩阵的行列式。|
|`np.linalg.inv(a)`|矩阵的逆。|
|`np.linalg.solve(A, b)`|求解线性方程组 $Ax = b$。|

---

## 8. I/O 操作

|**函数**|**描述**|
|---|---|
|`np.save()`|将单个数组以 `.npy` 格式保存到文件。|
|`np.load()`|从 `.npy` 文件中加载数组。|
|`np.savetxt()`|将数组保存为纯文本格式（如 CSV）。|
|`np.loadtxt()`|从纯文本文件加载数组。|
